

<?php 
/*
檔案：api/reg.php
功能：處理會員註冊請求，將表單資料寫入會員資料表。
*/

// 包含資料庫設定檔以存取 $Mem 物件
include_once "db.php";

// 1. 移除確認密碼欄位：因為資料庫只需儲存密碼，不需要存入額外用於確認的 pw2 欄位
unset($_POST['pw2']);

// 2. 資料寫入：直接將過濾後的 POST 資料陣列存入資料庫
$Mem->save($_POST);

/*
這段程式碼直接利用了 DB 類別中 save() 方法的自動化特性，省去了繁瑣的 SQL 語法編寫。
由於前端已經處理好欄位對應（例如 acc, pw, email 等鍵名與資料庫欄位名稱一致），
因此直接將 $_POST 傳入即可完成資料儲存。
*/
