<?php 
//檔案：api/chk_acc.php
//功能：檢查帳號是否存在，供前端註冊時進行即時判斷。
// 包含資料庫設定檔以存取 $Mem 物件
include_once "db.php";

// 透過 $_GET 接收前端傳入的帳號 (acc)，使用 count() 方法統計資料庫中該帳號的數量
// 若回傳值 > 0 代表該帳號已被註冊，回傳 0 則代表帳號可用
echo $Mem->count(['acc' => $_GET['acc']]);

//這段程式碼透過資料庫比對帳號是否重複，是前後端互動中非常基礎且核心的驗證機制。
//通常配合前端 AJAX 的 $.get 方法使用，協助實現「輸入帳號時立即檢查」的功能。
